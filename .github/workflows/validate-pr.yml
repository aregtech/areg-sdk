name: PR Validation

# >>> TEMPORARY 2026-08-17 -- disabled while cygwin-hunt.yml is investigating the
#     26_pubsubmix teardown hang. REVERT: delete this block and restore the original
#     triggers kept verbatim below. See SESSION-STATE.md, "Temporary CI state".
#
# on:
#   pull_request:
#     types: [opened, edited, reopened, synchronize]
on:
  workflow_dispatch:
# <<< TEMPORARY 2026-08-17

jobs:
  pr-check:
    runs-on: ubuntu-latest
    steps:
      - name: Verify PR Checklist
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prBody = context.payload.pull_request.body || '';
            const errors = [];

            if (!prBody.includes('[x] I have read CONTRIBUTING.md')) {
              errors.push('Please check the box confirming you have read CONTRIBUTING.md.');
            }
            if (!prBody.includes('Signed-off-by:')) {
              errors.push('Missing "Signed-off-by:" line in PR.');
            }
            if (errors.length > 0) {
              core.setFailed(errors.join('\n'));
            }
