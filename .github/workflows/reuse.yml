# SPDX-FileCopyrightText: 2020 Free Software Foundation Europe e.V.
# SPDX-License-Identifier: CC0-1.0

name: REUSE compliance check

# >>> TEMPORARY 2026-08-17 -- disabled while cygwin-hunt.yml is investigating the
#     26_pubsubmix teardown hang. REVERT: delete this block and restore the original
#     trigger kept verbatim below. See SESSION-STATE.md, "Temporary CI state".
#
# on: [push, pull_request, workflow_dispatch]
on: [workflow_dispatch]
# <<< TEMPORARY 2026-08-17

permissions:
  contents: read

jobs:
  reuse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: REUSE Compliance Check
        uses: fsfe/reuse-action@v6
