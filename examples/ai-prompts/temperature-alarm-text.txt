You are working in the areg-sdk repository at /mnt/c/projects/areg-sdk.

Read the file examples/ai-prompts/temperature-alarm.md and carry out the task it describes, exactly as written.

Generate every file into ~/bench/tempalarm, a directory of its own outside this repository. It is a project in its own right, the way an application built on areg normally is.

Environment, so you do not waste effort discovering it:
- a configured build tree of the SDK already exists at build/, with the framework already compiled. Build your project against it rather than fetching a second copy of the SDK.
- python3 is the interpreter here.

Two constraints on top of what that file says:
- Do not modify anything inside this repository. Everything you write goes under your target directory.
- Do not run any git command.

Everything else -- what to build, how to find it, what to verify before reporting, and what to report -- is in that file. Follow it.