=== EDIT THE THREE VALUES BELOW. NOTHING ELSE IN THIS FILE. ===============

Then paste this whole file as your prompt.

  task      what the software must do, and the checklist you are scored on.
            One of the task files beside this one. It names no framework.
  project   a C identifier. The directory name and the CMake project name.
  areg-sdk  absolute path to a checkout that provides run_scenarios.py, the
            framework-neutral process runner both arms are scored with. It is
            the only thing taken from it, and it is read only.

This file is the gRPC wrapper, and it is the other half of the split: the task
file names no framework, and everything framework-specific is here. It is the
worked example of how to give the same task to a second framework -- copy it,
change "gRPC" to whatever you are measuring, rewrite the numbered steps for that
framework's normal workflow, and you have a comparable arm.

Nothing here is maintained against a gRPC release and nothing here is a
published benchmark result. It is a shape to copy.

--- PROMPT BEGINS BELOW THIS LINE ----------------------------------------

You are building an application with gRPC. These are the settings for this task:

  task      <task>
  project   <project>

gRPC and protobuf are installed on this machine: protoc and grpc_cpp_plugin are on
the PATH and the headers are on the system include paths. **Never install anything,
never build gRPC or protobuf from source, and never FetchContent either of them.** If
a header or a tool is missing, stop and report it.

**The directory this session started in is the project root.** Work there, exactly
there. Do not create a directory for the project and do not cd anywhere else.

The process runner the report is scored with is

  <areg-sdk>/tools/agent/run_scenarios.py

It is framework-neutral and is the same runner the comparison's other arm uses. Read
its --help once, and write the scenarios.json it describes. Nothing else under
<areg-sdk> is yours to open, and you never write into it: it is another framework's
checkout and has nothing to say about gRPC.

Do this:

1. Read <task>. It is the whole specification: what the two programs must do, how to
   prove it, and the acceptance checklist you are scored on. It names no framework on
   purpose.

2. Build it with gRPC in the normal way for C++ and CMake:

   - one .proto declaring the service between the two programs, and the messages it
     carries. Generate the stubs from it at build time; never hand-write or commit
     generated code.
   - a server program and a client program, each with its own entry point, in their
     own subdirectories.
   - the streaming or callback shape that fits "the user program is notified when a
     value changes" -- a server-streaming RPC, or repeated unary calls, as you judge
     best. The task says what must be observed, not how.
   - the state machine the task requires is yours to express: gRPC has no state
     machine of its own, so write one explicitly -- a state enum with a transition
     table or equivalent -- and not a chain of ifs over flags.

3. Carry it out end to end -- contract, code, build, run, fix, report. You are not
   finished when the code is written. You are finished when it builds and every
   scenario exits 0, or when the task file's stop rule tells you to stop and report
   instead.

4. End with the report the task file asks for -- the small table and no more. Do not
   measure anything to fill it in and do not guess a number.

Rules for this task, above anything else:

- It is a project of its own. Do not add it to any build file of another framework,
  and write no IDE or editor project files.
- **One class, one .hpp and one .cpp, named after the class.** Never put two
  components in one file and never define a class inside a main().
- **Never edit a generated file.** Change the .proto instead.
- Never commit anything, and never run a git command.
- Do not write a ReadMe.
