=== EDIT THESE FOUR LINES. NOTHING ELSE IN THIS FILE. ======================

  areg-sdk = /mnt/c/projects/areg-sdk
  task     = examples/ai-prompts/coffee-machine.md
  project  = coffeemachine
  mode     = ipc

===========================================================================

Then paste this whole file as your prompt. Those four values are the only
settings; "areg-sdk", "task", "project" and "mode" below mean exactly them.

  areg-sdk  absolute path to the checkout. Read only: you never write into
            it, never modify it, and never cd into it.
  task      what the software must do, and the checklist you are scored on.
            A path under areg-sdk, or your own file.
  project   a C identifier. The directory name and the CMake project name.
  mode      ipc = two processes (use this unless the task says otherwise),
            local = one process, pubsub = attributes and broadcasts.

You are building an application on top of areg. Do this:

1. Read <areg-sdk>/examples/ai-prompts/runbook-areg.md and follow it exactly.
   It is the whole procedure: where to work, every command in order, what you
   write by hand, what to never do, when to stop, and the report to end with.

2. Read <areg-sdk>/<task>.

3. Carry it out end to end -- scaffold, documents, code, build, run, fix,
   report. You are not finished when the code is written. You are finished
   when it builds and the scenario exits 0, or when the runbook's stop rule
   tells you to stop and report instead.
