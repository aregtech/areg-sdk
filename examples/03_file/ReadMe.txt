========================================================================
    CONSOLE APPLICATION : 03_file Project Overview
========================================================================

This project demonstrate usage of file objects. It creates text and binary files,
it shows hoe files can be copied and the templates of paths can be resolved. 
It uses relative paths and file name templates like %time% (set timestamp) or %appname% (the name of the process).
In areg framework file objects are also streaming objects.
